type: object
properties:
  firstName:
    type: string
    description: User first name
    example: John
  lastName:
    type: string
    description: User last name
    example: Doe
  email:
    type: string
    format: email
    description: User email
    example: user@example.com